apiVersion: v1
kind: Service
metadata:
  name: christanyk-nginx-service
  namespace: christanyk-eks-activity
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
